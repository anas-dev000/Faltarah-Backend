
## 🚀 ملف أوامر المشروع (Prisma + Fastify + PostgreSQL)

> تقدر تحفظ ده كـ `commands.md` في المشروع.

---

### 🧱 إعداد المشروع لأول مرة

```bash
# 🟢 تثبيت كل الحزم المطلوبة من package.json
# اعملها أول مرة بعد ما تسحب المشروع من GitHub
npm install
```

---

### ⚙️ أوامر Prisma الأساسية (إدارة قاعدة البيانات)

```bash
# 📦 إنشاء قاعدة البيانات وتطبيق أول migration
# استخدمها أول مرة لما تبني الداتا بيز من الصفر
npx prisma migrate dev --name init
```

```bash
# 🧩 إعادة إنشاء Prisma Client بعد أي تعديل في schema.prisma
# استخدمها لو عدّلت الـ Models (جداول) أو العلاقات
npx prisma generate
```

```bash
# 📊 فتح واجهة رسومية لمعاينة البيانات (Prisma Studio)
# استخدمها لو عايز تدخل بيانات أو تشوف الجداول بسهولة
npx prisma studio
```

```bash
# 🧱 إنشاء Migration جديدة بعد تعديل schema.prisma
# استخدمها لما تضيف أو تغيّر أي Model
npx prisma migrate dev --name add_new_table
```

```bash
# 🗑️ حذف قاعدة البيانات بالكامل (للإعادة من الصفر)
# استخدمها بحذر لو عايز تمسح الداتا كلها وتبدأ من جديد
npx prisma migrate reset
```

---

### 🧠 أوامر التطوير (السيرفر)

```bash
# 🚀 تشغيل السيرفر في وضع التطوير مع auto-reload
# استخدمها دايمًا أثناء البرمجة
npm run dev
```

```bash
# 🧩 تشغيل السيرفر بدون nodemon (يدويًا)
# استخدمها لو عايز تشغله في وضع الإنتاج (أو من Docker)
node server.js
```

---

### 🧰 أوامر PostgreSQL (في حالة أردت تفتحها يدويًا)

```bash
# 🟢 تشغيل PostgreSQL محليًا (لو مش شغال)
# في Windows: من services
# في Linux/Mac:
sudo service postgresql start
```

```bash
# 🔍 التأكد من إن PostgreSQL شغال
psql -U postgres -l
```

```bash
# 🧑‍💻 الدخول إلى PostgreSQL CLI
# استخدمه لو عايز تدخل أوامر SQL يدويًا
psql -U postgres
```

```sql
-- 📜 عرض كل الداتابيز
\l

-- 🔄 التبديل إلى قاعدة بيانات faltaraDB
\c faltaraDB

-- 📋 عرض كل الجداول
\dt

-- 📄 عرض البيانات داخل جدول
SELECT * FROM "users";
```

---

### 🧹 أوامر لحل المشاكل الشائعة

```bash
# 🧼 حذف مجلد node_modules وإعادة تثبيت الحزم من البداية
# استخدمها لو حصلت أخطاء غريبة في npm أو prisma client
rm -rf node_modules package-lock.json
npm install
```

```bash
# ⚙️ إعادة توليد Prisma Client بعد فشل التحميل
# استخدمها لو ظهر Error: @prisma/client did not initialize yet
npx prisma generate
```

```bash
# 🧩 إعادة مزامنة قاعدة البيانات مع الـ schema من جديد
# استخدمها لو حصلت تغييرات مش مطبقة في الجداول
npx prisma migrate dev
```
